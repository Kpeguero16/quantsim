# Base URL for every API call. Points at the gateway, which fronts the auth
# and market-data services on one origin -- the frontend never talks to
# :8081 or :8082 directly. Defaults to this value when unset, so `npm run
# dev` works with no setup. See SPEC.md 2.3.
VITE_API_BASE_URL=http://localhost:8080
